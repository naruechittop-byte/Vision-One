-- VisionOneV2 foundation schema
-- Applied to Supabase project cpubahnvluadzquavowx on 2026-05-01.
-- Full migration is already applied in Supabase.
-- Keep this file as the repo reference for future schema tracking.

-- Core tables created:
-- clinics, profiles, patients, appointments, queue_items, visits, eye_exams,
-- diagnosis_master, visit_diagnoses, medications, prescriptions, prescription_items,
-- procedure_master, visit_procedures, invoices, invoice_items, payments,
-- inventory_items, stock_movements, documents, follow_ups, activity_logs.

-- Important: future schema changes should be added as new timestamped migration files,
-- not by editing this foundation marker file.
